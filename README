# monitor

Monitor polls a JMX server and adds the result to a db.  It also adds a http based viewer on the results.

## Usage

  lein deps

Start the http server

  lein ring server

Start the importer

  lein run
